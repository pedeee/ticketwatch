# Core dependencies for ticketwatch
requests>=2.31.0
beautifulsoup4>=4.12.2
python-dateutil>=2.8.2
playwright>=1.40.0

# Optional for macOS notifications
# terminal-notifier (install separately with: brew install terminal-notifier)